function solve(array) {
    for (const name of array) {
        let person={
            'name':name
        }
        console.log(`Name: ${person.name} -- Personal Number: ${person.name.length}`);
    }

}
